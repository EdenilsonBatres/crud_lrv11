comandos interesantes
php artisan make:model empleados -mcr
el mcr crea el controlador, el modelo y la migracion

despues de crear el modelo, darle forma al archivo de migracion, con el comando php artisan migrate ejecutamos}
el comando y este procede a crear la tabla en la base de datos sql
